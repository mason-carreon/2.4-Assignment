{
    let x="This is a let variable"; // This is a block variable
    console.log (x);
    var y="This is a var variable"; // This is a global variable
    }
    console.log (y);

    let a = 5;
    let b = 6;

    let z = a + b; // This is a mathematical operations with variables.

    console.log ("The total is " + z);

    {
        const car = {type:"Mustang", model:"302", color:"red"}; // This is a object variable
        console.log ("The car is a " + car.type);
         }

         let text = "This is a string using the \"let\" variable"; // This is a string

         console.log (text);